---
description: rsync Baylio → ~/backups/agencyflow/<timestamp>/baylio/
---

Run `bash scripts/qa/backup.sh`. Report the destination path and size.

Note: daily 03:00 backup already runs (see `~/backups/agencyflow/`). This command is for on-demand snapshots before risky changes.
